dist: trusty
sudo: false
language: c

addons:
  sonarcloud:
    - organization: Dovantech Github
    - token:
       secure: #Token

script:
 - sonar-scanner
 - cmake .
 - cmake --build . -- -j2
 - ctest
 
Cache:
  directories:
  $HOME/.sonar/cache
  $HOME/.gradle
 

